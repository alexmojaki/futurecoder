setup:
  addons:
    - plan: cloudamqp
build:
  docker:
    web: Dockerfile
    worker: Dockerfile
release:
  command:
    - ./deployment-tasks.sh
  image: worker
run:
  worker: python -m main.worker
